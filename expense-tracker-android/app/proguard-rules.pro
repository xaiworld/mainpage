# Add project specific ProGuard rules here.
# This project ships with minification disabled by default (see app/build.gradle.kts),
# so these rules only take effect if you turn isMinifyEnabled back on.
